{
  "name": "@gc-eta/model",
  "version": "0.1.0",
  "private": true,
  "description": "Priority-date prediction model. No UI dependencies, so it can be unit-tested and backtested in Node independently of the app.",
  "type": "module",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/test/",
    "estimate": "npm run build && node dist/src/cli.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
